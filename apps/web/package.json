{
  "name": "@expedition/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:sites": "vite build && vite build --config vite.worker.config.ts && node ../../scripts/stage-sites-build.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@expedition/game-data": "workspace:*",
    "@expedition/pixi-renderer": "workspace:*",
    "@expedition/progression-core": "workspace:*",
    "@expedition/shared-types": "workspace:*",
    "@expedition/simulation-core": "workspace:*",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  }
}
